"use strict";

const quantity = 5
console.log(typeof quantity) = 5; // 5

const pricePerDroid = 3000
console.log(typeof pricePerDroid) = 3000; // 3000


const quantity = 5
const pricePerDroid = 3000
console.log(quantity * pricePerDroid) = 3000; // 3000

const message = 'You picked ${productName}, price per item is ${pricePerItem} credits' ;
console.log(message);

